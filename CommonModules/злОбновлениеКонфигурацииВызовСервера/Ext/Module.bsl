#Область СлужебныеПроцедурыИФункции

Функция КаталогОбновлений() Экспорт
	
	Возврат злОбновлениеКонфигурации.КаталогОбновлений();
	
КонецФункции

Функция ВыполняетсяПроцессОбновления() Экспорт
	
	Возврат злОбновлениеКонфигурации.ВыполняетсяПроцессОбновления();
	
КонецФункции

Функция СостояниеОбновления() Экспорт
	
	Возврат злОбновлениеКонфигурации.СостояниеОбновления();
	
КонецФункции

Функция ИмяУстановщика(Версия = Неопределено, КлючСообщения = Неопределено) Экспорт
	
	Возврат злОбновлениеКонфигурации.ИмяУстановщика(Версия, КлючСообщения);
	
КонецФункции

Процедура ИзменитьСостояниеОбновления(Версия, Статус, Комментарий, ИмяФайла = Неопределено) Экспорт
	
	злОбновлениеКонфигурации.ИзменитьСостояниеОбновления(Версия, Статус, Комментарий, ИмяФайла);
	
КонецПроцедуры

Процедура РаспаковатьФайлОбновленияИзСообщения(КлючСообщения, Отказ, ТекстСообщения) Экспорт
	
	злОбновлениеКонфигурации.РаспаковатьФайлОбновленияИзСообщения(КлючСообщения, Отказ, ТекстСообщения);
	
КонецПроцедуры

#КонецОбласти
