
#Область ПрограммныйИнтерфейс

Процедура ПередНачаломРаботыСистемы() Экспорт
	
	КР_ЛокализацияИнформационнойБазы.ВыпонитьПервоначальноеЗаполнениеЛокализованныхДанных();
	// --> Евлахов Игорь Николаевич (Начало) 30.05.2024
	// Задача #4182
	злУстановитьРежимОсновногоОкнаКлиентскогоПриложения();
	// <-- Евлахов Игорь Николаевич (Конец) 30.05.2024
	
КонецПроцедуры

#КонецОбласти 

#Область СлужебныеПроцедурыИФункции

// --> Евлахов Игорь Николаевич (Начало) 30.05.2024
// Задача #4182
Процедура злУстановитьРежимОсновногоОкнаКлиентскогоПриложения()
    	
	ТекущийПользователь = ПользователиКлиент.ТекущийПользователь();
	
	Если ЗначениеЗаполнено(ТекущийПользователь) Тогда
		ИмяРеквизита = "злРежимОсновногоОкна";
		КодРежимаОсновногоОкна = ОбщегоНазначенияУТВызовСервера.ЗначениеРеквизитаОбъекта(ТекущийПользователь, ИмяРеквизита);
						
		Если КодРежимаОсновногоОкна = 1 Тогда
			НовыйРежим = РежимОсновногоОкнаКлиентскогоПриложения.РабочееМесто;
			КлиентскоеПриложение.УстановитьРежимОсновногоОкна(НовыйРежим);		
		Иначе
			НовыйРежим = РежимОсновногоОкнаКлиентскогоПриложения.Обычный;
			КлиентскоеПриложение.УстановитьРежимОсновногоОкна(НовыйРежим);
		КонецЕсли;
	КонецЕсли;
		
КонецПроцедуры
// <-- Евлахов Игорь Николаевич (Конец) 30.05.2024

#КонецОбласти
