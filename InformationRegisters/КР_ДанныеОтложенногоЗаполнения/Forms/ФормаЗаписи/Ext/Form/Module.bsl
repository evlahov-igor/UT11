// 26.05.2023 Марченко С.Н., КРОК, JIRA№A2105505-991

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	Если ЗначениеЗаполнено(Запись.GUID) Тогда 
		ТипДанныхСсылка = СтрЗаменить(Запись.ТипДанных, "Object.", "Ref.");
		Ссылка = XMLЗначение(Тип(ТипДанныхСсылка), Запись.GUID);
	Иначе
		Элементы.Ссылка.Видимость = Ложь;
	КонецЕсли;	

КонецПроцедуры
