
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
			
	ДокументОснование = Документы.РеализацияТоваровУслуг.ПустаяСсылка();
	Если Параметры.Свойство("Отбор") Тогда
		Параметры.Отбор.Свойство("ДокументОснование", ДокументОснование);
		Если ЗначениеЗаполнено(ДокументОснование) Тогда
			ТекстЗаголовка = НСтр("ru = 'Транспортные накладные (%Владелец%)'");
			ТекстЗаголовка = СтрЗаменить(ТекстЗаголовка, "%Владелец%", Строка(ДокументОснование));
			АвтоЗаголовок = Ложь;
			Заголовок     = ТекстЗаголовка;
		КонецЕсли;
	КонецЕсли;
	
	Список.Параметры.УстановитьЗначениеПараметра("ДокументОснование", ДокументОснование);
	Список.Параметры.УстановитьЗначениеПараметра("ДокументОснованиеЗаполнен", ЗначениеЗаполнено(ДокументОснование));
	
	СтандартныеПодсистемыСервер.УстановитьУсловноеОформлениеПоляДата(ЭтотОбъект, "Список.Дата", Элементы.Дата.Имя);

	// СтандартныеПодсистемы.ВерсионированиеОбъектов
	ВерсионированиеОбъектов.ПриСозданииНаСервере(ЭтотОбъект);
	// Конец СтандартныеПодсистемы.ВерсионированиеОбъектов
	
	// СтандартныеПодсистемы.ПодключаемыеКоманды
	ПараметрыРазмещения = ПодключаемыеКоманды.ПараметрыРазмещения();
	ПараметрыРазмещения.КоманднаяПанель = Элементы.ФормаКоманднаяПанель;
	ПодключаемыеКоманды.ПриСозданииНаСервере(ЭтотОбъект, ПараметрыРазмещения);
	// Конец СтандартныеПодсистемы.ПодключаемыеКоманды
	
	// ИнтеграцияС1СДокументооборотом
	ИнтеграцияС1СДокументооборот.ПриСозданииНаСервере(ЭтаФорма);
	// Конец ИнтеграцияС1СДокументооборотом
	
	// --> Евлахов Игорь Николаевич (Начало) 23.08.2023
	// Задача #3069
	злСобытияФормОценкаПроизводительности.ПриСозданииНаСервере(ЭтотОбъект, Отказ, СтандартнаяОбработка);
	// <-- Евлахов Игорь Николаевич (Конец) 23.08.2023
	
	// --> Евлахов Игорь Николаевич (Начало) 26.02.2024
	// Задача #3772 
	// Необходимо выполнять изменение запроса, до установки параметров
	злМодификацияФормы();
	// <-- Евлахов Игорь Николаевич (Конец) 26.02.2024
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	// --> Евлахов Игорь Николаевич (Начало) 23.08.2023
	// Задача #3069
	злСобытияФормОценкаПроизводительностиКлиент.ПриОткрытии(ЭтотОбъект, Отказ);
	// <-- Евлахов Игорь Николаевич (Конец) 23.08.2023
	
КонецПроцедуры

&НаКлиенте
Процедура ПриЗакрытии(ЗавершениеРаботы)
	Если Не ЗавершениеРаботы Тогда
		Оповестить("Запись_ТранспортнаяНакладная");
	КонецЕсли;
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыСписок

&НаКлиенте
Процедура СписокПриАктивизацииСтроки(Элемент)
	
	// СтандартныеПодсистемы.ПодключаемыеКоманды
	ПодключаемыеКомандыКлиент.НачатьОбновлениеКоманд(ЭтотОбъект);
	// Конец СтандартныеПодсистемы.ПодключаемыеКоманды
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаСервере
Процедура ВыполнитьКомандуНаСервере(ПараметрыВыполнения)
	ПодключаемыеКоманды.ВыполнитьКоманду(ЭтотОбъект, ПараметрыВыполнения, Элементы.Список);
КонецПроцедуры


&НаКлиенте
Процедура Подключаемый_ПродолжитьВыполнениеКомандыНаСервере(ПараметрыВыполнения, ДополнительныеПараметры) Экспорт
	ВыполнитьКомандуНаСервере(ПараметрыВыполнения);
КонецПроцедуры

// ИнтеграцияС1СДокументооборотом
&НаКлиенте
Процедура Подключаемый_ВыполнитьКомандуИнтеграции(Команда)
	
	ИнтеграцияС1СДокументооборотКлиент.ВыполнитьПодключаемуюКомандуИнтеграции(Команда, ЭтаФорма, Элементы.Список);
	
КонецПроцедуры
// Конец ИнтеграцияС1СДокументооборотом

#КонецОбласти


#Область СлужебныеПроцедурыИФункции

// СтандартныеПодсистемы.ПодключаемыеКоманды
&НаКлиенте
Процедура Подключаемый_ВыполнитьКоманду(Команда)
	ПодключаемыеКомандыКлиент.НачатьВыполнениеКоманды(ЭтотОбъект, Команда, Элементы.Список);
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОбновитьКоманды()
	ПодключаемыеКомандыКлиентСервер.ОбновитьКоманды(ЭтотОбъект, Элементы.Список);
КонецПроцедуры
// Конец СтандартныеПодсистемы.ПодключаемыеКоманды

#КонецОбласти

#Область злСлужебныеПроцедурыИФункции

#Область злПрочее

// Выполняет добавление, удаление, изменение: команд, реквизитов, элементов формы
//
&НаСервере
Процедура злМодификацияФормы()
		
	#Область злНастройкаДинамическогоСписка 
	
	// --> Евлахов Игорь Николаевич (Начало) 26.02.2024
	// Задача #3772
	СхемаЗапроса = Новый СхемаЗапроса;
	СхемаЗапроса.УстановитьТекстЗапроса(Список.ТекстЗапроса);
		
	ЗапросВыбориСхемыЗапроса 	= СхемаЗапроса.ПакетЗапросов[0];
	КолонкиСхемыЗапроса 		= ЗапросВыбориСхемыЗапроса.Колонки;
	ОператорыСхемыЗапроса 		= ЗапросВыбориСхемыЗапроса.Операторы[0];
	
	НовоеПолеВыборки = ОператорыСхемыЗапроса.ВыбираемыеПоля.Добавить("ТранспортнаяНакладная.КР_Отправитель");
	НовоеПолеВыборки = ОператорыСхемыЗапроса.ВыбираемыеПоля.Добавить("ТранспортнаяНакладная.КР_Получатель");
	НовоеПолеВыборки = ОператорыСхемыЗапроса.ВыбираемыеПоля.Добавить("ТранспортнаяНакладная.КР_ОбъемГруза");
	НовоеПолеВыборки = ОператорыСхемыЗапроса.ВыбираемыеПоля.Добавить("ТранспортнаяНакладная.КР_КоличествоКоробов");
	НовоеПолеВыборки = ОператорыСхемыЗапроса.ВыбираемыеПоля.Добавить("ТранспортнаяНакладная.КР_ЭкспедиторПартнер");
	НовоеПолеВыборки = ОператорыСхемыЗапроса.ВыбираемыеПоля.Добавить("ТранспортнаяНакладная.КР_ЧерезЦС");	
	
	// --> Евлахов Игорь Николаевич (Начало) 14.06.2024
	// Задача #3598
	НовоеПолеВыборки = ОператорыСхемыЗапроса.ВыбираемыеПоля.Добавить("ТранспортнаяНакладная.злСтатус");
	// <-- Евлахов Игорь Николаевич (Конец) 14.06.2024
	
	// Результат модификации
	Список.ТекстЗапроса = СхемаЗапроса.ПолучитьТекстЗапроса();
    // <-- Евлахов Игорь Николаевич (Конец) 26.02.2024
	
	#КонецОбласти
			
	#Область злДобавлениеЭлементовФормы
	
	// --> Евлахов Игорь Николаевич (Начало) 14.06.2024
	// Задача #3598
	КР_МетодыМодификацииФорм.ВставитьЭлементФормы(ЭтотОбъект, "Список.злСтатус", Элементы.Список);
	// <-- Евлахов Игорь Николаевич (Конец) 14.06.2024
	
	// --> Евлахов Игорь Николаевич (Начало) 26.02.2024
	// Задача #3772
	КР_МетодыМодификацииФорм.ВставитьЭлементФормы(ЭтотОбъект, "Список.КР_Отправитель", Элементы.Список);
	КР_МетодыМодификацииФорм.ВставитьЭлементФормы(ЭтотОбъект, "Список.КР_Получатель", Элементы.Список);	
	КР_МетодыМодификацииФорм.ВставитьЭлементФормы(ЭтотОбъект, "Список.КР_ЭкспедиторПартнер", Элементы.Список);
	КР_МетодыМодификацииФорм.ВставитьЭлементФормы(ЭтотОбъект, "Список.КР_ОбъемГруза", Элементы.Список);
	КР_МетодыМодификацииФорм.ВставитьЭлементФормы(ЭтотОбъект, "Список.КР_КоличествоКоробов", Элементы.Список);	
	КР_МетодыМодификацииФорм.ВставитьЭлементФормы(ЭтотОбъект, "Список.КР_ЧерезЦС", Элементы.Список);
	// <-- Евлахов Игорь Николаевич (Конец) 26.02.2024
		
	#КонецОбласти
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти
