
#Область ПроцедурыОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	ОбновлениеИнформационнойБазы.ПроверитьОбъектОбработан(Объект, ЭтотОбъект);
	
	Если Объект.Ссылка.Пустая() Тогда
		ЗаполнитьФормуПоОбъекту();
	КонецЕсли;
	
	// --> Евлахов Игорь Николаевич (Начало) 04.03.2024
	// Задача #3874	
	злПриСозданииНаСервере(Отказ, СтандартнаяОбработка);
	// <-- Евлахов Игорь Николаевич (Конец) 04.03.2024

КонецПроцедуры

&НаСервере
Процедура ОбработкаПроверкиЗаполненияНаСервере(Отказ, ПроверяемыеРеквизиты)
	
	ДанныеОбъекта = РеквизитФормыВЗначение("Объект");
	ДанныеОбъекта.ПроверитьУникальность(Отказ);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ЗаполнитьФормуПоОбъекту()
	
	ЗаполнитьЗначенияСвойств(Объект, Параметры.ЗначенияЗаполнения);
	
КонецПроцедуры

#КонецОбласти


#Область злОбработчикиСобытийФормы 

&НаСервере
Процедура злПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	злМодификацияФормы(Отказ, СтандартнаяОбработка);
		
КонецПроцедуры

#КонецОбласти

#Область злСлужебныеПроцедурыИФункции

&НаСервере
Процедура злМодификацияФормы(Отказ, СтандартнаяОбработка)
		
	#Область ЭлементыФормы
	// << 24.08.2022, Маскаев П.Ю., КРОК, Jira№ A2105505-324
	// КР_Маркируется
	КР_МаркируетсяЭлемент = КР_МетодыМодификацииФорм.ВставитьЭлементФормы(
		ЭтотОбъект, "Объект.КР_Маркируется", ЭтотОбъект);
	КР_МаркируетсяЭлемент.ПоложениеЗаголовка = ПоложениеЗаголовкаЭлементаФормы.Право;
	// >> 24.08.2022, Маскаев П.Ю., КРОК, Jira№ A2105505-324
	
	// --> Евлахов Игорь Николаевич (Начало) 04.03.2024
	// Задача #3874
	злМаркировкаОстатков = КР_МетодыМодификацииФорм.ВставитьЭлементФормы(
		ЭтотОбъект, "Объект.злМаркировкаОстатков", ЭтотОбъект);
	злМаркировкаОстатков.ПоложениеЗаголовка = ПоложениеЗаголовкаЭлементаФормы.Право;
	
	злМаркировкаДляМаркетплейсов = КР_МетодыМодификацииФорм.ВставитьЭлементФормы(
		ЭтотОбъект, "Объект.злМаркировкаДляМаркетплейсов", ЭтотОбъект);
	злМаркировкаДляМаркетплейсов.ПоложениеЗаголовка = ПоложениеЗаголовкаЭлементаФормы.Право;

	злМаркировкаДляРозницы = КР_МетодыМодификацииФорм.ВставитьЭлементФормы(
		ЭтотОбъект, "Объект.злМаркировкаДляРозницы", ЭтотОбъект);
	злМаркировкаДляРозницы.ПоложениеЗаголовка = ПоложениеЗаголовкаЭлементаФормы.Право;
	// <-- Евлахов Игорь Николаевич (Конец) 04.03.2024
	
	#КонецОбласти
	
КонецПроцедуры

#КонецОбласти

